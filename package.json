{
  "name": "node-addon-api-leak",
  "version": "0.0.0",
  "description": "node addon api leak",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Tests can only be run manually from the command line.\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fivdi/node-addon-api-leak.git"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "dependencies": {
    "bindings": "^1.3.0",
    "node-addon-api": "^1.2.0"
  },
  "author": "fivdi",
  "license": "MIT",
  "gypfile": true
}

